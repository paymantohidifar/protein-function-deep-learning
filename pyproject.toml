[project]
authors = [{name = "Payman Tohidifar", email = "payman.tohidifar@gmail.com"}]
dependencies = []
name = "pfdl"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
pfdl = { path = ".", editable = true }

[tool.pixi.tasks]
